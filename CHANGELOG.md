# 更新日志

## v1.5.0 (2026-09-21) — 教材精编笔记、填空题、真导出、复习中心与全新配色

### 笔记结构（教材精编）
- 笔记从「字幕要点平铺」升级为教材式层次：章 → 小节（定义/讲解/推导/例题/代码/对比/结论/要点）→ 内容块（正文、公式、推导步骤、列表、代码、表格、引用），章末含本章要点与小结，文首有「本课考点」
- 公式独立成块并以 LaTeX 存储，网页与导出统一 KaTeX 排版；表格、代码、推导步骤各有专属样式
- 旧版笔记自动兼容显示（读取时实时转换，不改动数据库）

### 自测
- 删除证明/思考卡题型，新增**填空题**：题干空位逐个输入框，提交即时判分，错空标红；等价答案（大小写、空白、中英文标点，多个等价说法用 / 分隔）均判对，不消耗 AI
- 题型固定为单选、判断、填空、计算（计算仍走 AI 批改）

### 导出（真正的归纳版，不再是字幕转存）
- **PDF 重写**：调用系统 Edge/Chrome 无头打印，排版与网页一致；KaTeX 公式、Mermaid 脑图、表格、代码块、关键帧截图全部渲染
- **Word 重写**：Pandoc 转换，公式为可编辑的 Office 公式（OMML），自动生成目录，脑图降级为层级大纲；系统无 Pandoc 时自动下载便携版（约 30MB，仅首次）
- Markdown 按教材精编结构实时生成；XMind 按章→小节→要点组织

### 复习中心（新增导航页）
- 今日待复习/逾期任务一览（艾宾浩斯计划，附笔记标题与到期状态），一键标记完成、跳转笔记
- 全局错题本：跨所有笔记归集，错次统计、一键回到视频对应位置、掌握后移出待复习

### 视觉
- 全站配色专业重设计：墨青主色 + 暖纸背景的学术纸墨风，浅色/深色双套设计令牌，Element Plus 组件、脑图、时间戳、题型卡片统一
- 笔记小节按类型显示彩色徽标，重点小节琥珀色高亮

### 视频
- 关键帧抽帧清晰度 720P 提升至 1080P
- 播放器宽屏模式，新增清晰度切换提示（登录 B 站后可在播放器内选 1080P）

## v1.4.0 (2026-09-21) — 确定性 bug 修复与稳定性增强

### AI 答疑
- 修复中文输入法选词时回车误发送消息（composition 状态判断）
- 新增「停止生成」按钮（AbortController 中断 SSE 流）
- 回答支持复制、重新生成；流式渲染闪烁大幅减轻（Mermaid 围栏未闭合时不提前渲染）
- 回答中的 Markdown 列表/代码块/粗体正常排版；KaTeX 解析失败时回退显示原始公式而非红色报错
- 对话记录本地持久化（按笔记保存最近 40 条）
- 答疑上下文扩充：摘要全量 + 章节边界截取 12000 字 + 最近 10 轮历史，取消 150 字回答限制

### 公式
- 公式识别默认改用 DeepSeek 视觉模型（deepseek-flash），备选通义 qwen-vl-max、Kimi kimi-k2.6
- 关键帧与公式帧分目录存放，旧笔记帧路径保持兼容
- 视觉调用 3 线程并发；视频流缓存（6 小时），关键帧与公式识别复用同一次下载
- 前端公式页按帧配对展示（左截图、右公式与要点）

### 自测
- 移除简答题，新增「思考卡」题型：先自己作答（可留空），再展开参考答案自评是否掌握，不做 AI 判分
- 单选题支持本地即时判分（字母比对 + 文本兜底），判断题本地判分，计算题保留 AI 批改
- 修复判断题被错误归类为简答题的现存 bug；单选答案归一化（兼容模型返回完整文本）
- 错题本按「笔记+题干」去重、记录错误次数与掌握状态，支持单题/批量上报
- 题干、选项、解析统一走 LaTeX/Markdown 渲染

### 笔记与合集
- 修复脑图同秒节点 id 冲突与自环（重复时间戳自动加后缀），mindmap 语法不再被错误改写
- 修复合集单 P 生成时标题误用合集总标题；重复生成同一分 P 直接复用已有笔记，不重复扣费
- 时间戳无法解析时留空，不再伪造 00:00:00；修复空时间戳章节导致题目归章错误
- 合集支持取消任务（进行中的集跑完后停止）、并发集数可配置（1-4，默认 2）
- 知识图谱按 bvid+分 P 去重（复用集也纳入），大合集自动降低章节预算、上下文截断放宽到 60000 字
- 服务重启后自动恢复：生成中的笔记标记失败可重试、卡住的合集任务自动收尾

### 播放器
- 跳转防抖去重（350ms 内连续点击、±1 秒内重复跳转不再重载 iframe），加载中显示遮罩

### 工程
- 模型清单更新：DeepSeek deepseek-v4-pro / Kimi kimi-k2.6 / 通义 qwen-plus / Ollama qwen2.5:7b，三家视觉模型齐全
- Whisper 模型进程内缓存（多集不再重复加载）、开启 VAD 过滤静音
- requirements 显式声明 av、去除重复依赖、yt-dlp 下限提升
- start.bat 按源码与构建产物时间戳增量重建前端，依赖更新自动安装
- 版本号统一为 1.4.0，健康检查接口返回版本

## v1.3.0 (2025-xx-xx) — Word/XMind 导出 + 流式答疑 + 关键帧

### 新增
- Word(DOCX) 导出：带样式的 Word 学习笔记，支持章节标题/时间戳加粗/生词表格，可嵌入关键帧截图
- XMind 导出：思维导图一键下载为 .xmind 文件（章节 + 知识点树）
- 笔记关键帧：按章节时间戳提取视频截图（无需视觉模型），嵌入笔记页与 Word/PDF 导出
- AI 答疑流式输出：SSE 逐字实时显示，长回答不再截断，体验更流畅
- SKILL.md：Claude Code / Cursor / Codex 等 AI 助手一键部署与操作指南

## v1.2.0 (2025-xx-xx) — 设计打磨与体验优化

### 体验
- 浅色/深色切换动画：从按钮位置**圆形蔓延至全屏**（View Transitions API，自动回退）
- 笔记/题目/公式/答疑全部支持 **LaTeX 排版渲染**（KaTeX，不再是纯字符）
- 公式识别升级：**均匀取帧覆盖全片**（不再只取开头），并同时提取**板书要点**兜底
- AI 答疑修复：回车/按钮**发送失败 bug**（事件对象误传）、抽屉布局重构（输入框固定底部、消息区独立滚动、长图可横向滑动）
- 前端设计体系：主色/圆角/阴影统一、卡片悬浮微交互、毛玻璃吸顶导航、渐变 Hero、按钮动效、深色模式全套适配

### 工程
- 新增零基础一键启动脚本：Windows 双击 `start.bat` / macOS·Linux `./start.sh`（自动装依赖、构建、启动、开浏览器）
- README「快速开始」重写为小白教程（含常见报错对照表）
- 网络健壮性：系统代理不可用时自动直连（LLM 与视频下载全链路）

## v1.1.0 (2025-xx-xx) — 交互升级 + 阶段3完整交付

### 新增
- 合集批量异步队列：后台逐集生成、进度实时展示、已完成集自动复用缓存
- 全课程知识图谱：自动梳理章节关联与前置/后续关系，节点可点击跨集跳转
- 全课程考点地图：考点按 必考/掌握/了解 分级汇总
- 多模态公式识别：关键帧提取（PyAV，无需 ffmpeg）+ 视觉大模型识别板书公式转 LaTeX
- AI 答疑：基于笔记上下文的对话，支持 Mermaid 图示化回答
- 逐题批改：每道自测题独立「判断」按钮 + AI 讲解，错题带讲解入错题本
- 双布局播放器：左右分栏（分隔条可拖动 + 笔记面板独立滚动）/ 视频置顶吸顶
- 浅色/深色模式一键切换（自动记忆）
- 脑图升级：箭头式 graph TD，节点隐藏时间戳、点击跳转视频
- B站 Cookie 配置（字幕/受限视频）、播放器高清参数、在B站打开当前片段
- 模型配置页：供应商 Key 官方网址入口、分区整理
- 笔记自动同步保存 Markdown 文件（backend/data/notes/）
- 生成进度基于真实耗时，切换页面不再重置

### 修复
- 生成笔记失败 'tuple' object has no attribute 'get'（SUBJECTS 命名冲突）
- 长视频合集误下载全部音频（whisper noplaylist 缺失）
- 无 CUDA 机器转写报 cublas64_12.dll 缺失（固定 CPU 推理）
- 自测选择题选项互相干扰（radio name 隔离）
- 时间戳正则转义错误、毫秒时间戳误判

### 优化
- 导出文档美化：Markdown 含目录/元信息，PDF 结构化排版（彩色章节、生词表格、页码）
- 历史笔记：筛选、自动刷新、人性化时间、失败重试
- 首页：字幕预览折叠、最近笔记入口
- 笔记页：章节目录快跳、失败页重试/删除

## v1.0.0 (2025-xx-xx) — 首个可用版本

- 视频解析（单视频/合集/分P）、官方字幕抓取、faster-whisper 本地转写
- 分学科结构化笔记（通用/英语/数理/计算机/文科）+ 思维导图
- 阶梯自测（基础/中档/拔高）、错题本、AI 复盘、艾宾浩斯复习计划
- DeepSeek/Kimi/通义千问/Ollama 四供应商统一封装
- Markdown/PDF/Anki/CSV 导出
- Docker Compose 一键部署 + 原生启动
